# Scribble

A real-time collaborative drawing application built with React and InstantDB. Create drawings together with others in real-time with smooth brush strokes and intuitive controls.

## TODO

- [x] установил и настроил линарию!
- [x] Внутри комнаты есть кнопка "Начать", но она ПОКА НЕ РАБОТАЕТ
- [x] Создание комнат в статусе подготовки
- [x] В списке всех комнат видно название комнаты и её статус
- [x] Внутри комнаты можно менять своё имя
- [x] Подключение других игроков по имени комнаты
- [x] Есть кнопка покинуть комнату
- [x] При подключении к комнате предлагают выбрать своё имя
- [x] Хост
  - [x] по умолчанию создатель
  - [x] может кикать других игроков
- [x] параметры новой игры
  - [x] количество раундов
  - [x] количество слов на выбор
  - [x] время на рисунок
- [x] хост может начать игру (пока без таймера и перехода ходов, начинаем пользоваться gameState)
  - [x] фиксируется список игроков в массив
  - [x] переключается страница в рисование
- [x] реанимировать состояние игры
  - [x] корректное стартовое состояние игры
    - [x] генерируются слова
    - [x] кто-то назначен рисовальщиком
    - [x] игроки видят чат/хост/инстрмуенты согласно своим ролям
    - [x] выбор слова режим рисования (пока без таймера)
  - [x] рисование линий!
    - [x] топик или что-то еще, нужно определиться и сделать
    - [x] полностью работает!
- [x] сообщения в чат
  - [x] просто сообщения в чат
  - [x] отгадывания
    - [x] "X почти отгадал"
    - [x] "x отгадал!"
- [x] создавать рисунок при выборе слова (слово, автор, количество отгадываний merge, статус in-progress/done)
  - [x] записывать ссылку на рисунок в игровое состояние currentDraw
  - [x] тот кто рисует, транслирует рисунок через топик, но каждую нарисованную линию записывает в рисунок через транзакцию
  - [x] игроки при открытии страницы единоразово качают рисунок из базы
  - [x] отгадки записываются в ~~рисунок~~ состояние игры игроками самостоятельно
  - [x] и если есть все отгадки, то завершает ход и отправляет сообщение в чат
- [x] если все отгадали, то рисование завершается
  - [x] сообщение "раунд закончен!"
  - [x] рисунок сохраняется
  - [x] видно кто следующий рисует
  - [x] тот кто рисует - выбирает слова
- [x] обновить схему и типы
  - [x] более адекватное состояние игры
  - [x] норм фиксированный список игроков
  - [x] обновить код и починить все ошибки типов
  - [x] gameProgress
- [x] мелкий рефакторинг
  - [x] сторы для всех рисунков
- [x] системные сообщения
  - [x] пользователь видит поле ввода, может отправлять сообщение и видит сообщение в списке
  - [x] "слово выбрано!"
  - [x] при создании игры
  - [x] "выбирает слово"
- [x] таймер
  - [x] рисовальщик устанавливает таймер
  - [x] рисовальщик по истечению времени закрывает рисунок до того как все отгадали
- [x] clue
  - [x] показывать подсказку
  - [x] функция которая дает подсказки clues(secret, drawTime): {time, clue}[]
  - [x] улучшать подсказку (до 30% первую половину времени, потом еще 30% на четверть ремени)
- [x] окно выбора нового слова
  - [x] видно результаты
- [x] игровой процесс
  - [x] следить за количеством сыгранных раундов
- [x] финал!
  - [x] показывать результаты: очки, имена, выделять текущего игрока
- [x] перепроверить список игроков
  - [x] перейти на правильный локальный id
  - [x] игроки видят список игр в которых участвовали
  - [x] ~~в игру записываются все игроки со статусами (играет, кикнут, вышел)~~
  - [x] ~~как-то связать локальный ID со снапшотом~~
- [x] "сервер"
  - [x] считает сколько игроков отгадали и меняет состояние игры
- [x] поле ввода отгадок с \_ и открытыми буквами

## До релиза

Нужно доделать, чтобы выпустить первую публичную версию:

- [x] алгоритм подсчета очков
  - [x] очки за отгадку (по скорости) + очки рисовальщику
  - [x] ~~показывать очки в списке игроков~~ (новый дизайн списка их не показывает)
- [x] финал: можно создать ещё одну игру (с теми же игроками) — кнопка "В лобби"
- [x] UI: "игра не найдена" для несуществующей/закрытой комнаты
- [x] человечки: шесть форм + личный цвет, редактор в лобби и профиле
  - [x] формы и статусы по дизайн-системе 3 (плитка роли, жетон, черта "вы")
  - [x] схема запушена, аватар сохраняется в $users.avatar
- [x] вёрстка игровых экранов: общая сетка на десктопе (`--col-main`, `--row-head`, `--row-under`), фиксированный чат на мобилке (`--chat-mobile`)
  - лобби и главную не трогали, там своя вёрстка
- [ ] поделиться рисунком — кнопка у картинки, открывает отдельную страницу
      рисунка: он в красивой рамке с подписью, как табличка в галерее — кто
      нарисовал, какое слово, когда, сколько собрал реакций. Ссылка живёт
      сама по себе, открывается без входа в игру
  - [ ] придумать фишки: подпись автора его человечком и цветом, паспарту и
        тень как у настоящей картины, «нарисовано за 47 секунд», отгадавшие
        как посетители выставки
  - [ ] `navigator.share` на телефоне, «скачать PNG» на десктопе (холст уже
        рисуется в canvas — `toBlob` даёт картинку почти даром)
  - [ ] превью в мессенджерах: og:image нужна **статической картинкой** по
        адресу, а фронт лежит на gh-pages и рендерить её на лету некому.
        Либо генерить PNG при сохранении рисунка и складывать в хранилище,
        либо отдавать og-страницу отдельным воркером. Решить до того, как
        делать саму страницу — от этого зависит, где она живёт
- [x] ластик (кисть цветом бумаги, шире в 1.5 раза)
- [x] не пускать пользователя в группу если он был кикнут. Показаывать сообщение об этом при попытки зайти (`party.kicked`)
- [ ] что-то крашится если открываю дропдауны в лобби (на мобилках или планшете, но не всегда). Не воспроизводится; дропдауны там — нативный `<select>` (`components/Select.tsx`), так что подозрение на что-то вокруг: фокус, ресайз вьюпорта под клавиатуру/пикер, фоновый холст `BackgroundDoodle`
- [x] при создании комнаты на мгновенье мигает кнопка «Вернуться в игру» (флаг `party.$enteringRoom`). `handleCreate` ждёт `createNewParty`, live-query `$allMyParties` обновляется раньше, чем сработает `navigate` строкой ниже: в этот кадр `location` ещё `/scribble`, `unfinished` уже есть, `inRoom` ещё `false` — и `PageLayout` рисует баннер. То же самое в `handleJoin`
- [x] убрать 10 секунд, добавить 120
- [ ] разные проверки, защиты и необычные случаи:
  - [x] закрывать комнату, если все вышли (последний вышедший ставит статус finished)
  - [x] запрещать стартовать пока в комнате только хост (кнопка заблокирована + подсказка, проверка продублирована в `startParty`)
  - [ ] модалка "Начать игру?", а текущую кнопку "Начать игру" переименовать в "Готово". Модального окна в проекте пока нет вообще — надо решить, заводить ли общий компонент
  - [x] автоматически выбирать первое слово, если ничего не выбрано за 60 секунд. Правила про 15 секунд и «минимум 3 секунды показа» отменили: таймер просто появляется в последние 30 секунд
  - [ ] кик игрока после старта игры (как?)
  - [ ] Давай небольшую задержку перед переходом в выбор слова, когда все угадали. Чтоб у художника было немоног времени завершить что начал (убираем палитру и инструменты, показываем сообщение что все угадали). Недорисованные линии (не успел отпустить мышку/палец) должны сохраниться.
  - [ ] как-то обрабатывать завершение игры до начала - сейчас открывает "финал", но нужно наверное просто редиректить на главную
  - [ ] не использовать длинные слова для генерации имени комнаты
  - [x] закрывать комнату если после закрыли вкладку — лобби без единого изменения 2 часа сервер закрывает сам
  - [ ] игра и комната — сейчас это одна сущность `party`, и из-за этого «закрыть» значит разное в разных местах: финал партии, брошенное лобби, кнопка хоста. Подумать, что разделить: комната живёт долго и в ней несколько игр, игра — одна запись со своим прогрессом. С этим же связаны пункты про «В лобби» и про рисунки одной комнаты
- [ ] перевести на английский, настройка в профиле
- [ ] добавить больше слов, английские слова
- [x] почитсить футер, убрать всякие dev штуки (остался только build, панель Dev — только в dev-сборке)

## После релиза (backlog)

Можно отложить на потом, не блокирует первый релиз:

- [ ] заливка (умная, прощающая дырки)
- [ ] догадки оверлеем поверх холста на широких экранах (`GuessOverlay` пока только для мобилки, где чат уехал наверх): те же пузыри, но по очереди в разных углах — чтобы и на десктопе можно было не смотреть в чат
- [x] оценка рисунка: в окне выбора слова миниатюра прошлого хода и реакции (😂 😮 🔥), жать можно сколько угодно раз — каждое нажатие +1. В базе лежат по игрокам (`paintings.reactions`), потому что пишутся merge-ом; на экране сумма
- [ ] реакции видно только в профиле и в окне выбора слова: ни в галерее финала, ни на странице «Картины» их нет
- [ ] страница комнаты: все рисунки одной партии с реакциями, словами и авторами — сейчас рисунки партии видно только в финале мелкой сеткой, а после «В лобби» они и оттуда пропадают. Связь `paintings.party` для этого уже есть
- [ ] одна комната — несколько игр подряд («В лобби»), и в профиле их рисунки лежат одной группой. Если начнёт мешать — счётчик игр на партии, его же ставить рисунку при создании
- [ ] разобраться с вёрсткой очков игроков: в квадратном окне выбора слова список растянут, под ним пустота
- [ ] финал: видно все рисунки, сыгранные за игру
- [ ] кнопка выхода/кика во время игры (софтделит из снапшота игроков)
- [x] настоящий бэкенд-таймер, не зависящий от клиента рисующего (`backend/server.ts`)
- [x] бекенд: сервер подписывается на новые сообщения в комнатах со статусом in-progress
- [x] таймер на выбор слова: по истечении `chooseWordTime` (60 сек) сервер выбирает первое слово за игрока. Это был последний способ подвесить партию навсегда — закрыть вкладку на выборе слова. На экране обратный отсчёт появляется только в последние 30 секунд
- [ ] если хост вышел из комнаты, то им назначается кто-то другой (с минимальным id?)
- [ ] цензурить отгаданное слово, если его написали снова
- [ ] presence комнаты (в схеме объявлена, но не используется): статус "отключён" у человечка и "X печатает…" в чате
- [ ] человечек подпрыгивает на новое сообщение от своего игрока
- [ ] ховер/тап по человечку: имя, очки, статус (сейчас только нативный title)
- [ ] тесты на calculateTurnPoints и calculateTotalScores
- [x] тест на переход раундов: правило вынесено в `nextTurn` (`src/utils.ts`), тест прогоняет партию целиком
- [ ] почистить словарь: в игре 1097 уникальных слов из трёх списков (`dictionaries/ru.ts`), но рисуются не все — абстракции («надежда», «обслуживание»), прилагательные и наречия («узкий», «пасмурно»). Выбор из нескольких слов лечит это лишь отчасти

## Features

- **Real-time Collaboration**: Multiple users can draw simultaneously with instant synchronization
- **Smooth Drawing**: Powered by `perfect-freehand` for natural, pressure-sensitive strokes
- **Touch Support**: Works on both desktop and mobile devices
- **Drawing Tools**:
  - Color picker
  - Adjustable brush sizes
  - Undo functionality
- **Player Figures**: каждый игрок выбирает форму и цвет человечка, статус в игре показывает плитка и жетон
- **Background Doodle**: в лобби можно порисовать прямо по фону страницы и выгрузить каракули в PNG
- **Responsive Design**: Optimized for various screen sizes

## Tech Stack

- **Frontend**: React 19 with TypeScript
- **State Management**: Effector
- **Real-time Database**: InstantDB
- **Drawing Engine**: perfect-freehand, lazy-brush
- **Routing**: Wouter
- **Build Tool**: Vite
- **Styling**: CSS

## Prerequisites

- Node.js (version 16 or higher)
- npm or yarn package manager

## Installation

1. Clone the repository:

```bash
git clone <repository-url>
cd scribble
```

2. Install dependencies:

```bash
npm install
```

3. Configure InstantDB:
   - The app uses InstantDB for real-time synchronization
   - Current configuration uses a demo ID in `src/config.ts`
   - For production, you'll need to set up your own InstantDB instance

## Development

Start the development server:

```bash
npm run dev
```

The application will be available at `http://localhost:5173` (or the port shown in your terminal).

### Служебные страницы

Ссылок на них в интерфейсе нет — футер почистили, — но роуты живые. Локально
то же самое на `http://localhost:5173`.

| Страница                                                  | Что там                                         |
| --------------------------------------------------------- | ----------------------------------------------- |
| [/words](https://joxter.github.io/scribble/words)         | весь словарь, можно прятать слова               |
| [/paintings](https://joxter.github.io/scribble/paintings) | все нарисованные картины                        |
| [/all-rooms](https://joxter.github.io/scribble/all-rooms) | все комнаты со статусами                        |
| [/dev](https://joxter.github.io/scribble/dev)             | моки игровых экранов (вёрстка без живой партии) |

## Available Scripts

- `npm run dev` - Start development server
- `npm run server` - Start the game server (needs `.env` with `INSTANT_APP_ID` and `INSTANT_APP_ADMIN_TOKEN`)
- `npm run build` - Build for production
- `npm run check-types` - Run TypeScript type checking
- `npm run deploy` - Build and deploy to GitHub Pages
- `npx instant-cli@latest push` - Push new schema for Instand DB

## Deploy (backend)

Сервер ведёт все таймеры. **Без него игра встанет**: как только время на рисунок
истекает, ход переключает именно он, клиент это больше не делает. Он же через
минуту выбирает слово за игрока, который завис на выборе, и закрывает лобби,
в котором два часа ничего не менялось.

Отсчёты сервер держит в памяти, а не в базе: после рестарта идущий ход и
брошенное лобби получают полный срок заново. Часы клиента в расчётах не
участвуют вообще — сервер пишет своё `startedAt` в `gameState`, чтобы таймер на
экране совпадал с тем, по которому он сам и сработает.

Локально:

```bash
cp .env.example .env   # заполнить из instantdb.com/dash
npm run server         # или: docker compose up -d --build
```

**Инстанс должен быть один на приложение.** Защита от повторного завершения хода
живёт в памяти процесса, так что второй сервер на том же `INSTANT_APP_ID`
продублирует события конца хода и перевыберет слова. Гоняешь локальный `npm run
server` — погаси контейнер на дроплете, или используй отдельное приложение.

|         |                                                             |
| ------- | ----------------------------------------------------------- |
| Дроплет | `134.209.227.168`, рядом чужие сервисы                      |
| Каталог | `/sites/scribble`                                           |
| Образ   | `ghcr.io/joxter/scribble/backend`, тег = sha коммита        |
| Портов  | не слушает, входящий трафик не нужен                        |
| Логи    | `docker compose -f docker-compose.prod.yml logs -f backend` |

На боксе ничего не собирается: CI (`.github/workflows/publish.yml`) прогоняет
тесты, собирает образ и кладёт в ghcr.io, дроплет только тянет. Сборки нет и в
CI — нода стрипает типы при загрузке, образ это зависимости плюс исходники.

### Первая установка

Порядок обязателен: `update.sh` тянет образ по sha текущего коммита, поэтому до
зелёного CI он откажется работать — это защита, а не помеха.

```bash
git clone https://github.com/Joxter/scribble.git /sites/scribble
cd /sites/scribble

# Админский токен обходит все правила доступа приложения — только сюда,
# не в репозиторий.
cp .env.example .env && nano .env
# Телеграм-бот тот же, что шлёт бэкапы sfw-game, и chat id там уже определён
grep '^TG_' /sites/sfw-game/.env >> .env
chmod 600 .env

./update.sh
```

### Обновление

```bash
cd /sites/scribble && ./update.sh
```

Пуш в master запускает CI; `update.sh` до появления образа отказывается
деплоить, вместо того чтобы молча выкатить вчерашний. HTTP-ручки у сервера нет,
поэтому вместо health-check скрипт смотрит, что контейнер через 5 секунд всё
ещё `running` — почти все способы выкатиться сломанным (битый `.env`, ошибка в
импорте, мёртвая подписка) роняют процесс сразу.

### Уведомления в телеграм

Через **@joxter_alert_bot** — того же, что шлёт бэкапы sfw-game (см.
`sfw-game/docs/deploy.md`, там же как достать токен, если `.env` соседа пропал).
Без `TG_BOT_TOKEN`/`TG_CHAT_ID` сервер просто молчит, ошибок это не создаёт.

| Событие                         | Что в сообщении                                   |
| ------------------------------- | ------------------------------------------------- |
| Игра началась                   | комната, сколько игроков и кругов                 |
| Игра закончена                  | комната, сыграно кругов, финальный счёт с именами |
| Подписка на InstantDB закрылась | сервер перезапускается, игры пока встали          |
| Сервер запустился               | подтверждение выката                              |

Отправка ничего не ждёт и никогда не бросает: телеграм лежит — игра идёт
дальше. О каждом ходе и о чате сознательно не сообщаем, это был бы шум.
После рестарта сервер не рассылает «игра началась» по уже идущим комнатам —
первый снапшот подписки это текущее состояние, а не новые игры.

## Key Components

### Canvas (src/drawing/Canvas.tsx)

The main drawing component that handles:

- Mouse and touch events for drawing
- Coordinate transformation for responsive design
- Real-time stroke rendering
- Integration with the drawing state

### Database (src/DB.ts & instant.schema.ts)

InstantDB integration for:

- Real-time data synchronization
- Canvas state persistence
- Multi-user collaboration

Рисунок связан со своей комнатой (`partyPaintings`, `paintings.party`).
Раньше эта связь жила только в `party.gameProgress`, а его обнуляет
`restartParty` («В лобби»): после второй игры в той же комнате все рисунки
первой оставались ничьи. Галерея финала по-прежнему читает `gameProgress` —
ей нужна текущая игра, а не всё, что нарисовано в комнате за её жизнь.

`backend/link-paintings.ts` — разовая миграция, проставившая связь старым
рисункам (44 из 119; остальные были в комнатах, которые успели перезапустить,
там связь восстанавливать не из чего). Скрипт идемпотентный, без `--apply`
только считает.

## Configuration

The app includes configuration options in `src/config.ts`:

- Canvas size settings
- Brush smoothing parameters
- Demo/party ID for database operations

## Deployment

The project is configured for GitHub Pages deployment:

```bash
npm run deploy
```

This will build the project and deploy it to the `gh-pages` branch.

## Development Notes

- Схема БД лежит в `instant.schema.ts`; после её изменения нужен `npm run push-instant`, иначе запись новых полей будет падать

- The app uses a demo configuration with a hardcoded party ID
- The drawing engine uses SVG paths for smooth, scalable strokes
- Touch events are handled for mobile compatibility
